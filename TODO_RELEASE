This TODO in THIS release:

o An option to add each card someone sends as an attachment to the Address Book
   (checking if the card is correctly written).
o Create a function that checks if a Card is correctly written.
